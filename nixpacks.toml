# nixpacks.toml - Next.js app in frontend/ subdirectory

providers = ["node"]

[variables]
NODE_ENV = "production"
NEXT_TELEMETRY_DISABLED = "1"
PORT = "3000"
HOSTNAME = "0.0.0.0"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd frontend && npm ci"]
cacheDirectories = ["~/.npm"]

[phases.build]
dependsOn = ["install"]
cmds = [
  "cd frontend && npm run build",
  "cp -r frontend/public frontend/.next/standalone/public || true",
  "cp -r frontend/.next/static frontend/.next/standalone/.next/static || true"
]
cacheDirectories = ["frontend/.next/cache"]

[start]
cmd = "node frontend/.next/standalone/server.js"
